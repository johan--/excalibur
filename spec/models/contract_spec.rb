require 'rails_helper'

RSpec.describe Contract, :type => :model do
  # before { @contract = FactoryGirl.build(:musharaka_contract) }

  # subject { @contract }

  # it { should respond_to(:begin_at) }
  # it { should respond_to(:begin_at) }
  # it { should respond_to(:end_at) }
  # it { should respond_to(:aqad) }
  # it { should respond_to(:ticker) }
  # it { should respond_to(:details) }
  # it { should respond_to(:deleted_at) }
  # it { should respond_to(:tender) }

  # describe "when saved" do
  # 	before(:each) { @contract.save }

  # 	describe "before create callback" do
  # 	  it "sets the contract into Musharaka class" do
  # 	  	expect(@contract.class.name).to eq "Musharaka"
  # 	  end
  # 	end
  # end

end