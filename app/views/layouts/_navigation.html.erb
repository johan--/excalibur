<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navigation">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to root_path, class: 'navbar-brand' do %>
        <%= app_name %> <span class="label label-success text-capitalize">alpha</span>
      <% end %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="navigation">
      <ul class="nav navbar-nav navbar-right">
        <% if !user_signed_in? %>
          <li><%= link_to "Masuk", new_user_session_path %></li>
          <li><%= link_to "Daftar", new_user_registration_path %></li>
        <% else %>
          <li>
            <%= link_to into_hub(current_user), class: 'navbar-brand' do %>
              <i class="glyphicon glyphicon-home"></i> 
            <% end %>              
          </li>          
          <% if current_user.admin? %>
            <li><%= link_to "Admin", admin_root_path %></li>
          <% end %>
          <li><%= link_to "Keluar", destroy_user_session_path, data:{method: :delete} %></li>
        <% end %>
        <li><%= link_to "Blog", posts_path %></li>
      </ul>
    </div>
  </div>
</nav>