<nav class="navbar navbar-default" id="classic-navbar" style="height: calc(0%); margin-bottom: 0px;">
  <div class="container">
    <div class="navbar-header">
      <%= link_to root_url(subdomain: ''), class: 'navbar-brand' do %>
        <%= ENV["SITE_NAME"] %> <span class="label label-success text-capitalize"></span>
      <% end %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav pull-right">
        <li><%= link_to "Markas", user_root_url(subdomain: '') if user_signed_in? %></li>
        <li><%= link_to "Blog", root_url(subdomain: 'blog') %></li>
        <li><%= link_to "Sign out", destroy_user_session_path, data:{method: :delete} if user_signed_in? %></li>
      </ul>
    </div>    
<!--     <%= render 'layouts/language_selector' %> -->
  </div>
</nav>