  <li class="list-group-item", style="padding-left: 5px">
    <div class="row toggle" id="dropdown-detail-<%= payment.id %>" data-toggle="detail-<%= payment.id %>">
      <div class="lists col-sm-3 col-xs-12 text-left">
        <span class="info_date">
          <i class="fa fa-calendar"></i> <%= payment.created_at.to_date %>
          <i class="fa fa-folder"></i> <%= payment.pay_code %>
        </span>
      </div>

      <div class="lists col-sm-4 col-xs-12 text-right">
        <span class="divider"></span>
        <span>
          <i class="fa fa-credit-card"></i> <%= payment.pay_day %>
        </span>
        <span class="status">
          <i class="fa fa-clock-o"></i> <%= payment.pay_time %>
        </span>
      </div>

      <div class="lists col-sm-3 col-xs-6 text-center">
        <span class="divider"></span>
        <span class="info_spent"><%= idr_money(payment.total)%></span>
      </div>

      <div class="lists col-sm-2 col-xs-6 text-center">
        <span class="divider"></span>
        <span class="badge">Detil <i class="fa fa-chevron-down"></i></span>
      </div>
    </div>

    <div id="detail-<%= payment.id %>">
      <hr></hr>
      <div class="container">
          <div class="fluid-row">
              <div class="col-sm-3 col-xs-12">
                <%= link_to "Koreksi", edit_biz_payment_path(payment) %>
              </div>
              <div class="col-sm-3 col-xs-12">
                <span class="divider"></span>
                <%= link_to "Lihat Kuitansi", biz_payment_path(payment) %>
              </div>
              <div class="col-sm-3 col-xs-12 text-center">
                <span class="divider"></span>
                <%=  %>  
              </div>                  
              <div class="col-sm-3 col-xs-12 text-center">
                <span class="divider"></span>
                <%=  %>
              </div>
          </div>
      </div>
    </div>
  </li>