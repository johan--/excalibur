<%= simple_form_for(@profile) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :about, label: "Biografi Singkat" %>
    <% if defined?(team) && team %>
      <%= f.simple_fields_for :details do |d| %>
        <%= d.input :anno, label: "Didirikan Pada Tahun" %>
        <%= d.input :founding_size, label: "Jumlah Pendiri" %>
        <%= d.input :offline_presence_types, as: :check_boxes, collection: offline_presence_options, label: "Infrastruktur Nyata Bisnis" %>
        <%= d.input :online_presence_types, as: :check_boxes, collection: online_presence_options, label: "Infrastruktur Online Bisnis" %>
      <% end %>
    <%= f.hidden_field :profileable_id, :value => @company.id  %> 
    <%= f.hidden_field :profileable_type, :value => @company.class.name  %> 
    <% elsif defined?(user) && user %>
      <%= f.simple_fields_for :details do |d| %>
        <%= d.input :last_education, collection: last_education_options, label: "Pendidikan Terakhir" %>
        <%= d.input :marital_status, collection: marital_status_options, label: "Status Pernikahan" %>
        <%= d.input :work_experience, label: "Pengalaman Kerja", required: false %>
        <%= d.input :industy_experience, label: "Pengalaman Industri/Bidang", required: false %>
      <% end %>
    <% end %>


    <div class="actions">
      <%= f.submit "Simpan", class: "btn btn-success" %>
    </div>    
  </div>

<% end %>