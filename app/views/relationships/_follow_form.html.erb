<%= div_for(followed, class: "follow-form") do %>
	<% if current_user.following?(followed) %>
		<%= render partial: 'relationships/unfollow', locals: { followed: followed } %>
	<% else %>
		<%= render partial: 'relationships/follow', locals: { followed: followed } %>
	<% end %>
<% end %>