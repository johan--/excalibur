<%= form_for(current_user.relationships.find_by(followed_id: followed.id,
	followed_type: followed.class.name),
			 html: { method: :delete }, remote: true, format: :js) do |f| %>
			 
	<%= f.button "- Favorit", class: "btn btn-sm btn-danger", data: {disable_with: "<i class='fa fa-spinner fa-pulse'></i> Menghapus arena dari favorit...."} %>
<% end %>