<li class="public-acitivty-item">
  <span class="glyphicon glyphicon-plus"></span>
  <small class="text-muted"><%= a.created_at.strftime('%H:%M:%S %-d %B %Y') %>
  <% if a.trackable %>
	<%= a.trackable.tenderable.name %> mengajukan <%= link_to a.trackable.barcode, business_tender_path(a.trackable.tenderable, a.trackable) %>
  <% else %>
	Proposal yang diajukan telah dihapus
  <% end %>
</li>